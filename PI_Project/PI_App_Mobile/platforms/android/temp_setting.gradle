android {
            defaultConfig {
                applicationId = "org.nativescript.SmartFeedback"

                if (false) {
                    minSdkVersion = false
                }

                if (false) {
                    targetSdkVersion = false
                }
            }
        }